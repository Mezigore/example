/// Условие получение скидки.
/// [count] - количество блюд.
/// [discount] - скидка.
class DiscountCondition {
  DiscountCondition(this.count, this.discount);

  final int count;
  final int discount;
}
